#1 - func : getOrigin [AxesMap] {
    moved from Graph(class) to AxesMap(class);
}

#2 - func : GetPoints [Graph] {
    changed to static function
}

#3 - param : domains [Integral] {
    change type from number[][] to number[];
}

#4 - func : getPathes [Integral] {
    now work with integral steps only;
}